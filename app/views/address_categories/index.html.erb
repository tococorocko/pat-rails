<% page_heading(t("addresses.head_title")) %>

<%= render 'layouts/quicklinks' %>

<div id="menu">
  <%= render 'partials/logo' %>
  <div class="content">
    <div class="inner">
      <h1><%= t("addresses.contatti") %></h1>
      <p>Tutte le famiglie con bambini piccoli e mamme durante la gravidanza hanno la possibilità di accedere a questi servizi per trovare risposte alle loro domande e ricevere informazioni e sostegno.</p>
    </div>
  </div>
  <div id="controls" class="nav">
    <% @address_categories.each do |category| %>
      <div class="control-link">
        <%= link_to category.name, address_category_path(category_id: category.id) %>
      </div>
    <% end %>
  </div>

  <div class="content extended">
    <div class="inner">
      <%= link_to medium_risk_path do %>
        <h2><%= t("addresses.medium_risk") %>?</h2>
      <% end %>
      <%= link_to high_risk_path do %>
        <h2><%= t("addresses.high_risk") %>?</h2>
      <% end %>
    </div>
  </div>
</div>
